# 人工 UI 验收清单

- 启动 exe：透明背景、无任务栏按钮、始终置顶和托盘图标。
- 悬停：任务表在左侧，首行中线对齐角色中线；长任务省略后悬停显示全名。
- 点击角色：下方新增框；离开 250ms 后收起，移到浮层不中断。
- 按住达到 200ms：立即进入拖动，可跨屏移动，重启后位置可见；200ms 内释放为短按。
- 托盘：双击、隐藏勾选、开机自启勾选、退出。
- 在 100%、125%、150% 缩放及多屏下检查文字不重叠、完成删除动画正常。
