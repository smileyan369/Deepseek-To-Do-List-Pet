# 测试说明

执行：

```powershell
python -m pytest -q
```

当前自动化测试共 45 项。核心测试覆盖任务排序、时间默认值、数据原子写入与损坏降级、位置边界、Windows 单实例锁、短按/长按阈值、动画状态机、联网搜索意图、Tavily 请求与解析、Bing HTML 解析、DuckDuckGo 回退、搜索失败检测、LPL 查询消歧与结果隔离；Qt 集成测试覆盖新增、回车保存、铅笔编辑、右键删除、到期颜色、完成动画、中文输入法、控件禁用、隐藏显示、全局快捷键注册/触发/冲突、浮层防越界、角色包替换、聊天 API 配置、`Enter` 发送、`Shift+Enter` 换行、搜索失败离线回答、请求取消、API Key 加密存储、SSE 流解析、聊天记忆、长回复气泡垂直滚动条和底部锚定气泡。人工验收清单在 `tests/manual_checklist.md`。
